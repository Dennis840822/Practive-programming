#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) 
{
	system("shutdown -s -t 50");//執行關機程式
	system("shutdown -a");//取消關機程式 
	return 0;
}
